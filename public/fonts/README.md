# LedCover fonts

Put commercial-use font files here when you want to bundle them with LedCover.

Recommended formats:

- `.woff2`
- `.woff`
- `.otf`
- `.ttf`

After adding a file, register it in `utils/fonts.ts` under `PROJECT_FONT_OPTIONS`.
